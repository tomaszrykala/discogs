<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.tomaszrykala.discogs"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.tomaszrykala.discogs.DiscogsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name="com.tomaszrykala.discogs.ui.detail.DetailActivity"
            android:theme="@style/AppTheme.Main.Detail">
        </activity>
        <activity
            android:name="com.tomaszrykala.discogs.ui.list.ListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Main.List">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>